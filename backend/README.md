# backend/

FastAPI application — project persistence, generation API, SSE streaming.
Built in (Project Persistence and Multi-Project Backend). The core
generation logic lives in the `cadless` package; this directory hosts the
web layer that exposes it.
