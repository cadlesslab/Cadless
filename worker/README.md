# worker/

Container build context for the isolated execution worker that runs
LLM-generated build123d code (OCCT toolchain). Built in
(Containerization and PoC Deployment). The worker runtime logic lives in
`cadless/worker.py`; this directory holds its Dockerfile and entrypoint.
